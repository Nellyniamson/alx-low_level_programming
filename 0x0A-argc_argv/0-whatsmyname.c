#include <stdio.h>
#include "main.h"

/**
 * main - print
 * @argc: count argurments
 * @argv: arguments
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
